{
  "name": "capstone-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Realtime gateway backend for Pi telemetry and web UI commands.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "ws": "^8.18.0"
  }
}
